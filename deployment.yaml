apiVersion: apps/v1
kind: Deployment
metadata:
  name: nodechat-deployment
  labels:
    app: my-node-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-node-app
  template:
    metadata:
      labels:
        app: my-node-app
    spec:
      containers:
      - name: nodechat
        image: srijaldocker/nodechat:1.0
        ports:
        - containerPort: 3000
